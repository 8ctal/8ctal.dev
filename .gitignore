# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment variables
.env
.env.local

# Impeccable — machine-local cache and per-developer overrides
# (.impeccable/config.json and design.json are shared/committed)
.impeccable/*.cache.json
.impeccable/config.local.json

# Reference component snippets pasted in for porting into real
# components — scratch material, not part of the shipped site
ref_components/